<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bisrihanafigmail.recyclingorganicwaste">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_backw"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_backw"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <service
            android:name=".IfonicNotificationService"
            android:enabled="true"
            android:exported="true"
            android:launchMode="singleTask"></service>

        <activity android:name=".Help"
            android:launchMode="singleTask"/>
        <activity android:name=".ChatRoomMe"
            android:launchMode="singleTask"/>
        <activity android:name=".PencairanDana"
            android:launchMode="singleTask"/>
        <activity android:name=".SettingLayout"
            android:launchMode="singleTask"/>
        <activity android:name=".WaitingLayout"
            android:launchMode="singleTask"/>
        <activity android:name=".FormInputUser"
            android:launchMode="singleTask"/>
        <activity android:name=".ShowID"
            android:launchMode="singleTask"/>
        <activity android:name=".DialogPilihan"
            android:launchMode="singleTask"/>
        <activity android:name=".BaseActivityHome"
            android:launchMode="singleTask"/>
        <activity android:name=".Login"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>